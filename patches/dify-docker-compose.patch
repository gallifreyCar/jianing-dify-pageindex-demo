diff --git a/docker/docker-compose.yaml b/docker/docker-compose.yaml
index 8ab3af9..858aacf 100644
--- a/docker/docker-compose.yaml
+++ b/docker/docker-compose.yaml
@@ -1122,8 +1122,8 @@ services:
       - api
       - web
     ports:
-      - "${EXPOSE_NGINX_PORT:-80}:${NGINX_PORT:-80}"
-      - "${EXPOSE_NGINX_SSL_PORT:-443}:${NGINX_SSL_PORT:-443}"
+      - "127.0.0.1:${EXPOSE_NGINX_PORT:-80}:${NGINX_PORT:-80}"
+      - "127.0.0.1:${EXPOSE_NGINX_SSL_PORT:-443}:${NGINX_SSL_PORT:-443}"
 
   # The Weaviate vector store.
   weaviate:
@@ -1609,6 +1609,40 @@ services:
     volumes:
       - ./volumes/unstructured:/app/data
 
+  jianing_mock:
+    image: python:3.11-slim
+    container_name: jianing_mock
+    restart: always
+    working_dir: /srv
+    command: ["python", "/srv/mock_backend.py"]
+    volumes:
+      - /Users/gallifreycar/Documents/Playground/dify_customer_service/mock_backend.py:/srv/mock_backend.py:ro
+      - /Users/gallifreycar/Documents/Playground/dify_customer_service/mock_docs:/srv/mock_docs:ro
+    ports:
+      - 127.0.0.1:8787:8787
+
+  pageindex_official:
+    image: python:3.11-slim
+    container_name: pageindex_official
+    restart: always
+    working_dir: /srv
+    command:
+      - /bin/sh
+      - -lc
+      - pip install --no-cache-dir -r /opt/pageindex/requirements.txt && python /srv/pageindex_service.py
+    environment:
+      CHATGPT_API_KEY: ${PAGEINDEX_LLM_API_KEY}
+      OPENAI_BASE_URL: ${PAGEINDEX_OPENAI_BASE_URL:-https://api.deepseek.com}
+      PAGEINDEX_MODEL: ${PAGEINDEX_MODEL:-deepseek-chat}
+      PAGEINDEX_DOC_ROOT: /srv/mock_docs
+      PYTHONPATH: /opt/pageindex
+    volumes:
+      - /Users/gallifreycar/Documents/Playground/PageIndex-official-src:/opt/pageindex:ro
+      - /Users/gallifreycar/Documents/Playground/dify_customer_service/pageindex_service.py:/srv/pageindex_service.py:ro
+      - /Users/gallifreycar/Documents/Playground/dify_customer_service/mock_docs:/srv/mock_docs:ro
+    ports:
+      - 127.0.0.1:8790:8790
+
 networks:
   # create a network between sandbox, api and ssrf_proxy, and can not access outside.
   ssrf_proxy_network:
